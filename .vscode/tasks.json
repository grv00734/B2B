{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "aegis: build extension",
      "type": "shell",
      "command": "npm run build:core && node esbuild.mjs",
      "options": { "cwd": "${workspaceFolder}/extension" },
      "problemMatcher": [],
      "group": "build"
    }
  ]
}
